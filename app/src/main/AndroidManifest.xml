<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Reading and starring the contacts whose calls ring while face down. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <!-- The app picker offers every app with a launcher icon. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SleepHistoryActivity"
            android:exported="false"
            android:label="@string/sleep"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".NightActivity"
            android:exported="false"
            android:label="@string/sleep"
            android:parentActivityName=".SleepHistoryActivity" />

        <activity
            android:name=".TodayActivity"
            android:exported="false"
            android:label="@string/today_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ExceptionsActivity"
            android:exported="false"
            android:label="@string/gets_through"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:label="@string/choose_apps"
            android:parentActivityName=".ExceptionsActivity" />

        <activity
            android:name=".LanguageActivity"
            android:exported="false"
            android:label="@string/language_title"
            android:parentActivityName=".SettingsActivity" />

        <service
            android:name=".ExceptionListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".FlipService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuously reads the accelerometer to detect the phone being placed face down on a flat surface, so Do Not Disturb can be toggled without the user unlocking the device." />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
