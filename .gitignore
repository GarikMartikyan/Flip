build/
.gradle/
.kotlin/
local.properties
*.apk
*.aab
.idea/
*.iml
captures/
.DS_Store

# Release signing. The keystore IS the app's identity — anyone holding it can
# ship an update that installs over Flip. Never commit either of these.
keystore/
keystore.properties

# The Play Console foreground-service demo lives on Drive; this is only the upload copy.
store-listing/flip-fgs-demo.mp4
